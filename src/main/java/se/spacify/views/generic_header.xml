<document>
    <hbox>
        <image src="${node.image_url}" width="32" height="32"/>
        <vbox>
            <label text="${node.name}" style="font-size: 14px; font-weight: bold;"/>
            <label text="${node.description}" style="font-size: 12px; color: #888888;"/>
        </vbox>
    </hbox>
</document>